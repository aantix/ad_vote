<%= link_to "Ad Tester. Zoom, Zoom.", root_path, :class => 'brand hidden-phone hidden-tablet' %>
<ul class="nav left-nav">
</ul>

<ul class="nav social hidden-phone ">
  <% if user_signed_in? %>
    <li>
      <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
    </li>
  <% else %>
    <li>
      <%= link_to "Login", new_user_session_path %>
    </li>
  <% end %>
</ul>